{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.22.0",
    "turbo": "^1.10.16"
  },
  "packageManager": "pnpm@8.12.1",
  "engines": {
    "node": ">=18"
  }
}
